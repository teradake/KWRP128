<UserControl
    x:Class="KWRP.Avalonia.Frontend.Views.EditorPage.LaneCreationPanelView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i18n="clr-namespace:KWRP.Frontend.Models.Localizer"
    xmlns:vm="using:KWRP.Avalonia.Frontend.ViewModels.EditorPage"
    Name="laneArrangementEditor"
    d:DesignHeight="650"
    d:DesignWidth="300"
    x:CompileBindings="True"
    x:DataType="vm:LaneCreationPanelViewModel"
    Focusable="True"
    mc:Ignorable="d">
    <UserControl.IsEnabled>
        <MultiBinding Converter="{x:Static BoolConverters.And}">
            <Binding Path="!IsDirectionSelectionMode.Value" />
            <Binding Path="!IsOptimizing.Value" />
        </MultiBinding>
    </UserControl.IsEnabled>

    <Grid>

        <TransitioningContentControl
            Width="{Binding $parent[UserControl].Bounds.Width}"
            Content="{Binding SettingPage, FallbackValue=setting page}"
            IsVisible="{Binding #setting.IsChecked}" />

        <ToggleButton
            Name="setting"
            Margin="4"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Background="Transparent"
            BorderBrush="Transparent"
            Content="setting"
            ZIndex="22">
            <ToggleButton.Styles>
                <Style Selector="ToggleButton:pointerover /template/ ContentPresenter#PART_ContentPresenter">
                    <Setter Property="Background" Value="#44ffffff" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                </Style>
                <Style Selector="ToggleButton:checked /template/ ContentPresenter#PART_ContentPresenter">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                </Style>
                <Style Selector="ToggleButton:pressed /template/ ContentPresenter#PART_ContentPresenter">
                    <Setter Property="RenderTransform" Value="Scale(0.95)" />
                </Style>
                <Style Selector="Image">
                    <Setter Property="Height" Value="30" />
                </Style>
            </ToggleButton.Styles>
            <Panel>
                <Image IsVisible="{Binding #setting.IsChecked}" Source="/Assets/Icons/close_red.png" />
                <Image IsVisible="{Binding !#setting.IsChecked}" Source="/Assets/Icons/setting_fill_gray.png" />
            </Panel>
        </ToggleButton>

        <Grid IsVisible="{Binding !#setting.IsChecked}" RowDefinitions="*, auto">





            <!--<CheckBox IsChecked="{Binding LaneCreated.Value}" HorizontalAlignment="Left" VerticalAlignment="Top" ZIndex="10"/>-->

            <!--  Navigationボタン  -->
            <Grid
                Grid.Row="1"
                Background="SeaShell"
                ColumnDefinitions="* *">

                <Button
                    Grid.Column="1"
                    Margin="0,10,10,10"
                    HorizontalAlignment="Right"
                    Command="{Binding NavigateNextCommand}">
                    <Button.Content>
                    <TextBlock>
                    <Run Text="{i18n:Localize LaneEditor.WorkAreaSetting}"/>&gt;
                    </TextBlock>
                </Button.Content>
                </Button>

            </Grid>

            <ScrollViewer Grid.Row="0">

                <Grid Grid.Row="0" RowDefinitions="auto, auto, auto, auto, auto, auto">

                    <!--  区割りパラメータUI  -->
                    <Label
                        Grid.Row="0"
                        Margin="0,5,0,10"
                        Padding="5"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Background="AntiqueWhite"
                        Content="{i18n:Localize LaneEditor.GenLaneParam}"
                        FontSize="20" />

                    <Grid
                        Grid.Row="1"
                        Margin="0"
                        RowDefinitions="auto, auto, auto, auto, auto auto auto auto auto auto auto">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" MinWidth="150" />
                        </Grid.ColumnDefinitions>
                        <Grid.Styles>
                            <Style Selector="TextBlock">
                                <!--<Setter Property="FontFamily" Value="MS Gothic"/>-->
                                <Setter Property="HorizontalAlignment" Value="Left" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                            <Style Selector="NumericUpDown">
                                <Setter Property="ShowButtonSpinner" Value="{Binding !$self.IsReadOnly}" />
                                <Setter Property="Background" Value="{Binding $self.IsReadOnly, Converter={StaticResource ReadOnlyToTransparentConverter}}" />
                                <Setter Property="FormatString" Value="0.00" />
                                <Setter Property="Increment" Value="0.05" />
                                <Setter Property="Classes.SafeMode" Value="{Binding !IsDevMode}" />
                            </Style>
                            <Style Selector="NumericUpDown.adaptiveon">
                                <Setter Property="Increment" Value="10.0" />
                            </Style>
                            <Style Selector="NumericUpDown.SafeMode">
                                <Setter Property="Minimum" Value="0" />
                            </Style>
                        </Grid.Styles>


                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.LaneWidth}" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.FrontOffset}" />
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.RearOffset}" />
                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.SidePrevOffset}" />
                        <TextBlock
                            Grid.Row="4"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.SideNextOffset}" />
                        <TextBlock
                            Grid.Row="5"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.LapWidth}" />
                        <TextBlock
                            Grid.Row="6"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.LaneChangeLength}" />
                        <TextBlock
                            Grid.Row="7"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.ProgressDirectionDegree}" />
                        <TextBlock
                            Grid.Row="8"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.RollerHeadingDirection}" />
                        <TextBlock
                            Grid.Row="9"
                            Grid.Column="0"
                            Text="" />

                        <NumericUpDown
                            Grid.Row="0"
                            Grid.Column="2"
                            IsReadOnly="True"
                            Value="{Binding LaneWidth.Value}" />
                        <NumericUpDown
                            Grid.Row="1"
                            Grid.Column="2"
                            IsReadOnly="False"
                            Value="{Binding FrontOffset.Value}" />
                        <NumericUpDown
                            Grid.Row="2"
                            Grid.Column="2"
                            IsReadOnly="False"
                            Value="{Binding RearOffset.Value}" />
                        <NumericUpDown
                            Grid.Row="3"
                            Grid.Column="2"
                            IsReadOnly="False"
                            Value="{Binding SidePrevOffset.Value}" />
                        <NumericUpDown
                            Grid.Row="4"
                            Grid.Column="2"
                            IsReadOnly="False"
                            Value="{Binding SideNextOffset.Value}" />
                        <NumericUpDown
                            Grid.Row="5"
                            Grid.Column="2"
                            Increment="0.01"
                            IsReadOnly="False"
                            Minimum="0.0001"
                            Value="{Binding LapWidth.Value}" />
                        <NumericUpDown
                            Grid.Row="6"
                            Grid.Column="2"
                            IsReadOnly="True"
                            Minimum="0.01"
                            Value="{Binding LaneChangeLength.Value}" />
                        <NumericUpDown
                            Grid.Row="7"
                            Grid.Column="2"
                            Classes.adaptiveon="{Binding #toggleDirection.IsChecked}"
                            IsReadOnly="False"
                            Value="{Binding ProgressDirectionDegree.Value}" />
                        <NumericUpDown
                            Grid.Row="8"
                            Grid.Column="2"
                            IsReadOnly="True"
                            Value="{Binding RollerHeadingDirectionDegree.Value}" />


                        <Button
                            Grid.Row="9"
                            Grid.Column="1"
                            Grid.ColumnSpan="2"
                            Margin="10"
                            HorizontalAlignment="Stretch"
                            Command="{Binding ChangeHeadingCommand}"
                            Content="{i18n:Localize LaneEditor.ReverseRollerDirection}" />

                        <TextBlock
                            Grid.Row="10"
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Text="{i18n:Localize LaneEditor.ProgressDirectionChange}" />
                        <ToggleSwitch
                            Name="toggleDirection"
                            Grid.Row="10"
                            Grid.Column="1"
                            Grid.ColumnSpan="3"
                            HorizontalAlignment="Center"
                            OffContent="±0.05 deg."
                            OnContent="±10 deg." />
                    </Grid>

                    <!--  ペアリングパラメータUI  -->
                    <Label
                        Grid.Row="2"
                        Margin="0,10"
                        Padding="5"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Background="Aquamarine"
                        IsVisible="{Binding !IsParallelMode.Value}"
                        Content="{i18n:Localize LaneEditor.GroupingParam}"
                        FontSize="20" />

                    <Grid
                        Grid.Row="3"
                        Margin="0"
                        IsVisible="{Binding !IsParallelMode.Value}"
                        RowDefinitions="auto, auto, auto, auto, auto auto auto auto auto auto">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" MinWidth="130" />
                        </Grid.ColumnDefinitions>
                        <Grid.Styles>
                            <Style Selector="TextBlock">
                                <!--<Setter Property="FontFamily" Value="MS Gothic"/>-->
                                <Setter Property="HorizontalAlignment" Value="Left" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                            <Style Selector="NumericUpDown">
                                <Setter Property="ShowButtonSpinner" Value="{Binding !$self.IsReadOnly}" />
                            </Style>
                        </Grid.Styles>

                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.MinLaneCount}" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.MaxLaneCount}" />

                        <NumericUpDown
                            Name="pairMinCnt"
                            Grid.Row="0"
                            Grid.Column="2"
                            FormatString="0"
                            IsReadOnly="False"
                            Maximum="{Binding #pairMaxCnt.Value}"
                            Minimum="1"
                            Value="{Binding PairMinCount.Value}" />
                        <NumericUpDown
                            Name="pairMaxCnt"
                            Grid.Row="1"
                            Grid.Column="2"
                            FormatString="0"
                            IsReadOnly="False"
                            Minimum="{Binding #pairMinCnt.Value}"
                            Value="{Binding PairMaxCount.Value}" />

                    </Grid>


                    <!--  平行作業選択用UI  -->
                    <Label
                        Grid.Row="4"
                        Margin="0,10"
                        Padding="5"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Background="Azure"
                        Content="{i18n:Localize LaneEditor. }"
                        IsVisible="{Binding IsParallelMode.Value}"
                        FontSize="20" />

                    <Grid
                        Grid.Row="5"
                        Margin="0"
                        IsVisible="{Binding IsParallelMode.Value}"
                        RowDefinitions="auto, auto, auto, auto, auto auto auto auto auto auto">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" MinWidth="130" />
                        </Grid.ColumnDefinitions>
                        <Grid.Styles>
                            <Style Selector="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Left" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                            <Style Selector="NumericUpDown">
                                <Setter Property="ShowButtonSpinner" Value="{Binding !$self.IsReadOnly}" />
                            </Style>
                        </Grid.Styles>

                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.BaselineWidth}" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.StartLane}" />
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.AreaPitch}" />
                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="0"
                            Text="{i18n:Localize LaneEditor.WrapAdjustment}" />

                        <NumericUpDown
                            Grid.Row="0"
                            Grid.Column="2"
                            FormatString="0"
                            IsReadOnly="False"/>
                        <NumericUpDown
                            Grid.Row="1"
                            Grid.Column="2"
                            FormatString="0"
                            IsReadOnly="False"/>
                        <NumericUpDown
                            Grid.Row="2"
                            Grid.Column="2"
                            FormatString="0"
                            IsReadOnly="False"/>
                        <NumericUpDown
                            Grid.Row="3"
                            Grid.Column="2"
                            FormatString="0"
                            IsReadOnly="False"/>

                    </Grid>

                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>